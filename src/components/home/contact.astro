---
import MailIcon from "@icons/mail_icon.svg"
import InIcon from "@icons/linkedin_icon.svg"
---

<div class="contact">
    <div class="contact_formular">
        <div class="text">
            <h3>Me contacter</h3>
            <p class="text_content">Contactez moi facilement avec mon adresse email. Je suis ouvert à toutes demandes. N'hésitez pas également à me suivre sur les réseaux sociaux pour me contacter.</p>
            <div class="social">
                <img src={MailIcon}/>
                <p>bourhis.clement29@gmail.com</p> 
                <a class="send" href="mailto:bourhis.clement29@gmail.com?subject=Prise%20de%20contact">Envoyer un e-mail</a>
            </div>
            <div class="social">
                <img src={InIcon}/>
                <p><a href="https://www.linkedin.com/in/clement-bourhis" target="_blank">@clement-bourhis</a></p>
            </div>
        </div>
    </div>
</div>

<style>
    .contact {
        width: 100vw;
        max-width: 1920px;
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        margin: auto;
    }

    .contact_formular {
        width: 50%;
        border-radius: 30px;
        padding: 50px;
        background: rgba(47, 47, 138, 0.6);
    }

    h3 {
        margin-bottom: 20px;
    }

    .text_content {
        width: 60%;
    }

    .social {
        display: flex;
        align-items: center;
        margin-top: 20px;
    }

    img {
        width: 40px;
    }

    .social p {
        margin: 0 20px 0 20px;
    }

    .send {
        padding: 10px;
        background: linear-gradient(151deg, #6993FF 15%, #af36ff 95%);
        cursor: pointer;
        border: inherit;
        display: flex;
        position: relative;
        justify-content: center;
        align-items: center;
        font-family: 'Poppins Regular';
        font-size: 10px;
        color: white;
        border-radius: 35px;
        background-color: rgba(124, 124, 124, 1);
    }

    .send::before {
        content:"";
        position:absolute;
        inset:0;
        padding: 1px;
        border-radius: 35px;
        background: linear-gradient(135deg, rgb(255, 255, 255) 20%, rgb(192, 240, 255) 35%, rgba(245,111,232,0) 60%);
        --grad: linear-gradient(red, red);
        -webkit-mask: var(--grad) content-box, var(--grad);
        -webkit-mask-composite: destination-out;
        mask-composite: exclude;
    }

    a {
        transition: color 0.2s ease-in-out;
    }

    a:hover {
        color:black;
    }

    @media screen and (max-width: 1000px) {
        h3 {
            text-align: center;
        }

        .text_content {
            width: 100%;
            text-align: center;
        }

        .social {
            flex-direction: column;
            margin: 30px 0 30px 0;
        }

        .social p {
            margin: 10px;
        }
    }
</style>